# 🎶 歌词显示效果深度微调手册 (Refinement Guide)

这份手册旨在帮助 alonewinds 完美主义地调优播放器歌词显示。手册涵盖了**播放详情页**与**桌面歌词**的所有视觉参数。

---

## 📂 核心文件索引
- **播放详情页逻辑**：[`useAmllLyric.js`](file:///h:/repositories/music-alonewinds/src/renderer/utils/compositions/useAmllLyric.js)
- **歌词核心处理 (包含空行过滤)**：[`lyric.ts`](file:///h:/repositories/music-alonewinds/src/renderer/core/lyric.ts)
- **播放详情页样式**：[`LyricPlayer.vue`](file:///h:/repositories/music-alonewinds/src/renderer/components/layout/PlayDetail/LyricPlayer.vue)
- **桌面水平歌词样式**：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricHorizontal/index.vue)
- **桌面垂直歌词样式**：[`LyricVertical/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricVertical/index.vue)

---

## 一、 播放详情页 (Play Detail)

### 1. 动态缩放与行间距 (JS 动力学)
> 修改文件：[`useAmllLyric.js`](file:///h:/repositories/music-alonewinds/src/renderer/utils/compositions/useAmllLyric.js)

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 |
| :--- | :--- | :--- |
| **激活行缩放倍率** | 第 71 行 | `(isActive && isZoomActiveLrc) ? 1.3 : 1.0` (1.3 即 130%) |
| **推开上下行力度** | 第 94 行 | `const extraSpace = (newScale - 1) * dom.clientHeight * 0.6` (0.6 -> 1.0 可推更远) |
| **下方额外呼吸感** | 第 95 行 | `dom.style.marginBottom = \`${extraSpace + 7}px\`` (+7 越大离下一行越远) |
| **模糊效果强度** | 第 85 行 | `blur = factor * 8` (8 为最大模糊像素值) |
| **非活动行透明度** | 第 86 行 | `Math.max(0.15, 1 - factor * 0.8)` (0.15 是最浅透明度) |

### 2. 静态布局与字号 (CSS)
> 修改文件：[`LyricPlayer.vue`](file:///h:/repositories/music-alonewinds/src/renderer/components/layout/PlayDetail/LyricPlayer.vue)

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 |
| :--- | :--- | :--- |
| **主歌词原文大小** | 第 359 行 | `font-size: 1.4em !important;` |
| **翻译/罗马音大小** | 第 367 行 | `font-size: 0.8em !important;` (位于 `.extended-font-lrc`) |
| **原文与翻译间距** | 第 363 行 | `.extended { margin-top: 2px; }` (数值越大间距越大) |
| **普通行高/间距** | 第 337 行 | `padding: calc(var(...) / 2) 1px;` (决定静止时的行间距) |

---

## 二、 桌面歌词 (Desktop Lyric)

### 1. 单词与单词之间的距离 (逐字模式)
> 修改文件：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout\LyricHorizontal/index.vue)

| 修改目标 | 位置 (约) | 修改方法 |
| :--- | :--- | :--- |
| **单词紧凑度** | 第 138 - 142 行 | 调整 `margin-left/right: -0.20em;`。**负值越大越紧凑**。 |
| **单词呼吸感** | 第 138 - 142 行 | 调整 `padding-left/right: 0.12em;`。数值越大单个单词越“胖”。 |

### 2. 当前行与其他行的距离 (激活态)
> 修改文件：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout\LyricHorizontal/index.vue)

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 |
| :--- | :--- | :--- |
| **桌面缩放倍率** | 第 253 行 | `transform: scale(1.45);` (1.45 表示缩放为 145%) |
| **向上推开距离** | 第 254 行 | `margin-top: calc(var(--line-gap) * 5.5);` (系数 5.5 越大离上行越远) |
| **向下推开距离** | 第 255 行 | `margin-bottom: calc(var(--line-gap) * 5.8);` (系数 5.8 越大离下行越远) |

### 3. 原文与翻译的距离
> 修改文件：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout\LyricHorizontal/index.vue)

| 状态 | 位置 (约) | 修改方法 |
| :--- | :--- | :--- |
| **播放中 (Active)** | 第 251 行 | `.active .extended { margin-top: 0px !important; }` (此处加值可推开) |
| **非播放 (Normal)** | 第 118 行 | `.extended { margin-top: calc(var(...) / 3); }` |

---

## 三、 垂直歌词模式 (Vertical Mode)
> 修改文件：[`LyricVertical/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricVertical/index.vue)

垂直模式的逻辑与水平模式完全对称，只是坐标轴变化：
- **行间距**：由 `margin-top/bottom` 变更为 **`margin-left/right`**。
- **原文与翻译间距**：由 `margin-top` 变更为 **`margin-right`** (约 121 行和 243 行)。

---

## 💡 调优建议
1.  **保存即生效**：如果你运行着 `npm run dev`，保存文件后浏览器/Electron 窗口会自动刷新。
2.  **!important 优先级**：在 Vue 组件样式中，有些动力学控制的样式需要加上 `!important` 才能确保覆盖成功（我已经帮你把关键位置都加好了）。
3.  **单位建议**：字号和垂直间距建议使用 `em` (相对于字体的单位) 或 `px` (绝对单位)；单词间距建议坚持使用 `em`。

---

## 四、 空白行自动清洗逻辑
为了保证显示效果不被无意义的空行干扰，系统在加载歌词时会自动执行清洗（移除只有时间戳而无实际歌词的行）。

- **修改文件**：[`lyric.ts`](file:///h:/repositories/music-alonewinds/src/renderer/core/lyric.ts)
- **处理入口**：`filterEmptyLines` 函数。
- **作用范围**：原文歌词、翻译歌词、罗马音歌词均会自动剔除空白行及 `//` 注释行。

---
*alonewinds, 极致的美感来源于每一像素的精准控制。*
