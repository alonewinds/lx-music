# 🎶 歌词显示效果深度微调手册 (Refinement Guide)

这份手册旨在帮助 Mr.alonewinds 完美主义地调优播放器歌词显示。手册已针对当前项目最新版本（基于 Electron 37+ / Vue 3）进行了校准，修正了行号及已应用的最佳视觉动力学参数。

---

## 📂 核心文件索引
- **播放详情页滚动与动效逻辑**：[`useAmllLyric.js`](file:///h:/repositories/music-alonewinds/src/renderer/utils/compositions/useAmllLyric.js) *(全新重构，引入了 Spring 动力学动画)*
- **歌词核心处理 (包含空行过滤)**：[`lyric.ts`](file:///h:/repositories/music-alonewinds/src/renderer/core/lyric.ts)
- **播放详情页样式**：[`LyricPlayer.vue`](file:///h:/repositories/music-alonewinds/src/renderer/components/layout/PlayDetail/LyricPlayer.vue)
- **桌面水平歌词样式**：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricHorizontal/index.vue)
- **桌面垂直歌词样式**：[`LyricVertical/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricVertical/index.vue)

---

## 一、 播放详情页 (Play Detail)

### 1. 动态缩放与行间距 (JS 动力学)
> 修改文件：[`useAmllLyric.js`](file:///h:/repositories/music-alonewinds/src/renderer/utils/compositions/useAmllLyric.js)
> 在该版本中，引入了 Spring 弹簧动力学（第 24-25 行），滚动和视差体验更加丝滑有重感。

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 | 当前默认值 |
| :--- | :--- | :--- | :--- |
| **激活行缩放倍率** | 第 74 行 | `let targetScale = isActive && isZoomActiveLrc ? 1.35 : 0.95` | `1.35` (非活动行设为 `0.95` 以制造景深) |
| **推开上下行力度** | 第 101 行 | `const extraSpace = (scale - 1) * dom.clientHeight * 0.8` | `0.8` (数值越大，非活动行被推得越开) |
| **下方额外呼吸感** | 第 102 行 | `dom.style.marginBottom = \`${extraSpace + 10}px\`` | `+10` (越大离下一行越远，配合 marginTop 呼吸感更佳) |
| **上方额外呼吸感** | 第 103 行 | `dom.style.marginTop = \`${extraSpace * 0.5}px\`` | `* 0.5` (让激活行向两边均匀推开) |
| **模糊效果最大阈值** | 第 79 行 | `Math.min(4, Math.pow(distance / (maxDistance * 0.6), 1.2) * 2.5)` | 最大模糊 `4`px，视差调节系数 `0.6`，幂指数 `1.2` |
| **非活动行透明度** | 第 94 行 | `opacity = Math.max(0.15, 0.65 - distRatio * 0.5)` | 基础透明 `0.65`，随距离最深淡出至 `0.15` |
| **插值缓动因子** | 第 83 行 | `const lerpFactor = 0.1` | `0.1` (数值越小过渡越平缓，越大越灵敏) |

### 2. 静态布局与字号 (CSS)
> 修改文件：[`LyricPlayer.vue`](file:///h:/repositories/music-alonewinds/src/renderer/components/layout/PlayDetail/LyricPlayer.vue)

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 | 当前默认值 |
| :--- | :--- | :--- | :--- |
| **主歌词原文大小** | 第 360 行 | `font-size: 1.4em !important;` | `1.4em` |
| **原文与翻译间距** | 第 364 行 | `.extended { margin-top: 4px; }` | `4px` (较旧版本微增以强化可读性) |
| **翻译/罗马音大小** | 第 368 行 | `font-size: 0.8em !important;` (位于 `.extended-font-lrc`) | `0.8em` |
| **普通行高/间距** | 第 337 行 | `padding: calc(var(--playDetail-lrc-font-size, 16px) / 2) 1px;` | 使用动态变量自适应，除以 `2` 决定静止时的间距 |

---

## 二、 桌面水平歌词 (Desktop Lyric - Horizontal)

### 1. 单词与单词之间的距离 (逐字模式)
> 修改文件：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricHorizontal/index.vue)

| 修改目标 | 位置 (约) | 修改方法 | 当前默认值 |
| :--- | :--- | :--- | :--- |
| **单词紧凑度** | 第 154 - 155 行 | 调整 `margin-left/right: -0.20em;`。**负值绝对值越大越紧凑** | `-0.20em` |
| **单词呼吸感** | 第 151 - 152 行 | 调整 `padding-left/right: 0.12em;`。数值越大单个单词越“胖” | `0.12em` |

### 2. 当前行与其他行的距离 (激活态)
> 修改文件：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricHorizontal/index.vue)

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 | 当前默认值 |
| :--- | :--- | :--- | :--- |
| **桌面缩放倍率** | 第 254 行 | `transform: scale(1.45);` (1.45 表示缩放为 145%) | `1.45` |
| **向上推开距离** | 第 255 行 | `margin-top: calc(var(--line-gap) * 5.5);` (系数 5.5 越大离上行越远) | `5.5` |
| **向下推开距离** | 第 256 行 | `margin-bottom: calc(var(--line-gap) * 5.8);` (系数 5.8 越大离下行越远) | `5.8` |

### 3. 原文与翻译的距离
> 修改文件：[`LyricHorizontal/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricHorizontal/index.vue)

| 状态 | 位置 (约) | 修改方法 | 当前默认值 |
| :--- | :--- | :--- | :--- |
| **播放中 (Active)** | 第 252 行 | `margin-top: 0px !important;` (强制缩短激活行内部翻译与原文的距离) | `0px !important` |
| **非播放 (Normal)** | 第 118 行 | `margin-top: calc(var(--line-extended-gap) / 3);` | 动态由 `line-extended-gap` 驱动 |

---

## 三、 桌面垂直歌词 (Desktop Lyric - Vertical)
> 修改文件：[`LyricVertical/index.vue`](file:///h:/repositories/music-alonewinds/src/renderer-lyric/components/layout/LyricVertical/index.vue)

由于排版变为从右到左、从上至下的垂直排版，动力学参数与水平模式对称但数值有所不同：

| 修改目标 | 位置 (约) | 关键代码 / 修改方法 | 当前默认值 |
| :--- | :--- | :--- | :--- |
| **垂直缩放倍率** | 第 253 行 | `transform: scale(1.25);` | `1.25` |
| **向右推开距离** | 第 254 行 | `margin-left: calc(var(--line-gap) * 3.0);` (右侧行被向左/右推开) | `3.0` |
| **向左推开距离** | 第 255 行 | `margin-right: calc(var(--line-gap) * 3.8);` | `3.8` |
| **原文与翻译间距(Active)**| 第 251 行 | `margin-right: 2px !important;` (让翻译更靠近原文) | `2px !important` |
| **原文与翻译间距(Normal)**| 第 121 行 | `margin-right: var(--line-extended-gap);` | 动态由 `line-extended-gap` 驱动 |

---

## 四、 空白行自动清洗逻辑
为了保证显示效果不被无意义的空行干扰，系统在加载歌词时会自动执行清洗（移除只有时间戳而无实际歌词的行）。

- **修改文件**：[`lyric.ts`](file:///h:/repositories/music-alonewinds/src/renderer/core/lyric.ts)
- **处理入口**：第 175 行的 `filterEmptyLines` 函数。
- **作用范围**：原文歌词、翻译歌词、罗马音歌词均会自动剔除空白行及 `//` 注释行。

---
*Mr.alonewinds, 极致的美感来源于每一像素的精准控制。*
