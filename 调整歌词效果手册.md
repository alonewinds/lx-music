要手动调整播放详情页歌词的粗细和字号，你主要需要关注以下两个文件：

1. 调整基础字体大小(CSS)

**文件路径：** 

src/renderer/components/layout/PlayDetail/LyricPlayer.vue

在这个文件的 <style>部分，你可以修改默认的数值：

字号控制 在```lrcFontSize```
计算属性中（约 160 行）：

```
const lrcFontSize = computed(() => {
  let size = appSetting['playDetail.style.fontSize'] / 100 // 这里从设置项读取
```

粗细控制 (CSS)

 ： 搜索font-lrc和.line-content  （约 327 行）：

  ```
  font-lrc {
    font-weight: 500; // 非当前行粗体
  }
  .active .font-lrc {
    font-weight: 700; // 当前行粗体
  }
  ```

------

### 2. 调整动态加粗和模糊效果 (JS)

由于我们使用了模拟 AMLL 的动力学渲染，**JS 中的设置会覆盖 CSS**。 **文件路径：** 

```
src/renderer/utils/compositions/useAmllLyric.js
```

在 applyEffects 函数中（约 38 行开始）：

- 手动调整粗细查找dom.style.fontWeight（约 58 行）：
 
  ```
  dom.style.fontWeight = isActive ? '700' : '500' // 修改这里的数字，700是加粗，900是极粗
  ```
- 缩放比例查找const scale（约 42 行）：

  ```
  const scale = isActive ? 1.05 : 1.0 // 改为 1.1 或更高可以增大当前行的放大效果
  ```
  
- 模糊强度查找blur = factor * 8（约 54 行）：

  ```
  blur = factor * 8 // 把 8 改大（如 12）会让模糊更强烈，改小则模糊更轻微
  ```





###  桌面歌词微调 

可以前往以下位置：

- 调整桌面歌词单字放大强度

  ```
  src/renderer-lyric/core/lyric.ts
  ```

   约 103 行，修改1.2这个保底值。

- 调整桌面歌词整行放大强度

  ```
  src/renderer-lyric/components/layout/LyricHorizontal/index.vue
  ```

   约 240 行，修改scale(1.45)

  垂直布局同理，位于 约 239 行

  ```
  LyricVertical/index.vue
  ```

  